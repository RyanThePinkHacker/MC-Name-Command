# Name Command
A mod for Minecraft that adds a name command.

## Syntax:

```
/item
	enchant
		(target)
			(enchantment)
				(level)
	give
		(target)
			(item)
				(count)
	name
		(target)
			(name)
```
